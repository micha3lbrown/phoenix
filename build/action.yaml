
name: 'Packer build template file'
inputs:
  templateFile:
    required: true
  varFile:
    required: true
  workingDir:
    required: false
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'check-circle'
  color: 'blue'